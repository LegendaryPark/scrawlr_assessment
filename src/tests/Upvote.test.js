// import { render, fireEvent } from "@testing-library/react";
// import UpvoteButton from "../components/common/UpvoteButton/UpvoteButton";

// test("toggle upvote selection", () => {
//   const { getByRole } = screen.getByRole(<UpvoteButton />);
//   const button = getByRole("button");
//   fireEvent.click(button);
//   // Expect some change in the DOM indicating the selection state
// });
